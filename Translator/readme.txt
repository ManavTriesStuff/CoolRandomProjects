Simple English to Hindi (languages can be changed as long as Google translate supports it) translator 
that takes a voice input through the microphone and returns TTS based voice output in the translated language.
This uses Python's google translate package to do the actual translation.
